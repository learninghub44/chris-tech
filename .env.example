# Copy this file to .env and fill in real values. Never commit .env.

# PostgreSQL connection string (required)
DATABASE_URL=postgres://user:password@localhost:5432/chris_tech

# API server
PORT=5000
NODE_ENV=development
LOG_LEVEL=info

# Optional: base path if the API is served behind a path prefix (e.g. /api)
BASE_PATH=
